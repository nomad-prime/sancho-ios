import SwiftUI

struct SanchoTheme {
    static let primaryColor = Color("SanchoPrimaryColor")
    static let accentColor = Color("AccentColor")
    static let backgroundTint = Color("SanchoBackgroundColor")
    static let cornerRadius: CGFloat = 8.0
    static let spacing: CGFloat = 8.0
}
